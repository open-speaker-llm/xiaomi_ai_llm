<a id="测试说明"></a>

<a id="2-人工测试"></a>

<a id="3-修改后跑哪些测试"></a>

<a id="4-提交前建议"></a>

# 验证方法：从主机回归到真实音箱

验证分为三层：主机自动化检查逻辑与配置，匹配设备上的独立测试检查 ABI 和模型行为，真人用例检查实际唤醒、播放和家居动作。前一层通过，不能替代后一层。

本页维护“怎样验证”；某个版本已经通过什么、仍缺什么，统一看[当前状态](docs/status.md)及其证据链接。

## 1. 自动化测试

在仓库根目录执行：

```sh
./scripts/run_tests.sh
```

脚本优先使用项目 `.venv/bin/python`，也可通过 `PYTHON=/path/to/python sh scripts/run_tests.sh` 指定环境。部分测试调用本机 C 编译器；Linux CPU 限额信号测试在 macOS 跳过，必须单列，不能记作通过。

| 领域 | 主要覆盖 |
|---|---|
| 服务端 | ASR 质量门控、LLM/TTS 流式分句、旧追问识别服务 |
| 播放 | 单句重试、部分音频拒收、按序原生补播、状态等待、失败不写完整历史 |
| 首轮判停 | final-only、同轮 quiet、旧轮拒绝记录、抢占与取消、模型重置、音频追平、管理器生命周期 |
| 连续追问 | 并发控制、空结果与旧文本隔离、同 session、输入补全与提示上限、物理唤醒交接 |
| 原生适配 | guard 文案分类、暂停所有权、hook 状态隔离、音量恢复、boot0 兼容 |
| 配置和脚本 | Shell 语法、模板可 source、关键推荐参数与默认值一致 |

## 2. 匹配固件的独立验证

原生组件修改还需在匹配设备的独立进程验证 JsonCpp/回调 ABI、提示音隔离、重复 `Dialog.Finish` 的正常消费，以及交接后旧指令的拒绝。构建和用例入口见 [native_asr 说明](device/native_asr/README.md)。

### 首轮判停的验证分层

1. 完成主机回归，单列平台跳过项。
2. 用固定依赖构建 ARM 包，核对 `package.sha256`；独立验证真实 ABI、模型重置、旧内核时钟、抢占和取消。
3. 真实模型回放覆盖正常输入、短时积压追平、过载拒绝、静默和下一轮恢复；耐久记录 PID、RSS、FD 与映射数量。
4. 再做 EP 现场用例，分别记录整机重启、断电冷启动与全天运行，不能从短时回放推断长期稳定。

固定依赖和安装基线见 [native_endpoint](device/native_endpoint/README.md)。不要把早期包的测试次数算到最终包，也不要把文件回放描述为真人现场验收。

## 3. 真实音箱人工验证

按 [tests/manual_native_first_cases.md](tests/manual_native_first_cases.md)逐项操作，记录实际输入、日志、用户听觉与设备动作。日志中的 ASR 文本不能单独证明灯真的开了；进程 healthy 也不能证明播报没有漏句。

覆盖原生成功、失败转 LLM、失败提示拦截、播放期间唤醒、双系统差异、上下文追问、静默退出、首轮停顿与异常恢复。旧 PCM + Mac 路线单独验收，不与当前原生追问结果混用。

## 4. 修改后选择对应检查

| 改动范围 | 必跑 |
|---|---|
| 服务端 LLM/TTS/ASR | 完整自动化测试 |
| 主客户端状态机或路由 | 自动化 + M1/M2/M3 |
| 播放 freeze/replay | 自动化 + M1/M2/M3/B3 |
| boot1 兼容、guard 或失败文案 | 自动化 + P3/S2，含重启及原生报时对照 |
| 首轮判停、路由记录或常驻管理 | 自动化 + EP1–EP7；模型/ABI 改动另需 ARM 独立测试及真实模型回放 |
| 追问相关 | 自动化 + F1/F2；boot1 另跑 F-NATIVE 和设备 ABI/提示音隔离；旧 PCM 路线跑 F-PCM |
| 仅文档 | 相对链接与标题锚点、Mermaid 渲染、命令运行位置、`git diff --check` |

## 5. 提交验证结果

说明对应提交或安装包、测试环境、通过/失败/跳过项，以及实机测试的边界。原始录音、私有配置、完整对话和固件资产不提交仓库，证据描述放入[历史记录](docs/history/README.md)。

文档修改后检查导航是否仍能走通；操作步骤迁移时保留必要的旧锚点。确认 `git status --short`，避免把本机产物一并提交。
