# 同源音频现场诊断：正常基线

用户于 2026-09-19 表示现场方便，已提前说明本轮保存最多 20 秒音箱输入。确认设备空闲、实验库及 runner 哈希与准备版本一致后，临时 setup 并运行 `trial capture 5`。精确 sequence 41 绑定后才提示说“苹果的种子为什么会发芽”，随后保持安静。用户确认只说一次、之后安静、无其他人声或音箱报错。

## 结果

- 目录 `trial.capture.5.97wgib`，dialog `54e7facc69cd3029a00a44dcc34fdb29`。
- 完整识别“苹果的种子为什么会发芽”，CLI 0，final/Finish 正常。EOF 到 final 日志 124 ms，未观察到 SDK TTS timeout。
- quiet 触发于累计音频 8870 ms、墙钟收音 8581 ms，最后确认 VAD 语音位置 6870 ms，没有触发 20 秒硬上限。
- 小米最后可用 partial 的 end offset 为 6600 ms；两者差 2270 ms 是句末等待的粗估，非人工逐帧标注。最后不同 partial 到实际 EOF 1979 ms。本次没有复现此前约 5 秒等待。
- 保存同一原生 ASR callback 的 PCM 284160 字节（8.88 秒）。比 VAD 决策多 10 ms，来自原始 60 ms callback 块的末尾；没有追加新一轮录音。`used=0`，未注入旧 PCM。

小米指令快照只有 partial，final/Finish 由原生探针日志、CLI 结果和状态交叉证明，不把快照末尾的 partial 叫作 final。

## 尾部与复现核对

主 VAD 在 6870 ms 转为阴性后，7200–7290、7780–7870、7960–8050 ms 各出现 90 ms 阳性，均未达到 120 ms 连续确认条件，未重置结束计时。模式 2/3 同位置为约 70 ms 阳性。与旧 A 中 9770–10570 ms 的 800 ms 阳性段不同，本样本没有对应的长片段，不能据此解释旧 A 的声音来源。

在音箱独立进程中读取此次保存的 PCM，运行相同版本 libfvad 和结束状态机：**34 个 VAD 状态变化全部与实时日志逐项一致**，仍在 8870 ms quiet，last_voice=6870 ms。测试前后原生服务 PID、sequence/dialog 和状态不变；没有启动麦克风、云端 ASR 或 LLM。这验证了此次诊断副本与实时检测输入的一致性，不证明此前原生固件从未串轮。

本地仅封装 WAV 和统计波形数值，未进行语音转写：7–8 秒 RMS 约 457，8 秒后约 393（16-bit 原始幅值），整秒均值接近 0；问题主体 5–6 秒 RMS 约 8795。这些数值不能单独识别声音来源或变成生产幅度阈值；没有声学标注，不称其为确定的风扇、呼吸、背景人声或 VAD 错误。

## 恢复与状态

录音完成后立即主动恢复。两个 init 哈希与 before 一致，客户端 SHA256 仍为 `70a7012f2c68c7ebc8e545d9481845068406a476dc4cff7e68808fe671f8f184`；armed/capture.armed/timer/busy 均无。复核 native_asr healthy、mipns 2484 / aivs 2423，进程不加载实验库。未更改判停参数、未部署普通唤醒首轮、未增加服务。

原始音频和日志在忽略目录 `tmp/asr-shadow-20260918/capture-trials/trial.capture.5.97wgib/`，只保留作已授权诊断，不提交仓库。关联证据：`capture-41-audit.json`、`capture-41-restore.txt`、`capture-41-health.txt`、`capture-41-vad-replay.txt`、`capture-41-replay-summary.json`。

本轮建立了可复核的正常基线，没有捕获此前的长尾异常。仍待解决：真实输入的开始资格、偶发句末拖延、此前重复文本根因，以及原始请求中的播放停顿。不能把这次正常样本当作上述问题已修复，也不以单次样本缩短阈值。
